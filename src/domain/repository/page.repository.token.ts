import { InjectionToken } from '@angular/core';
import { PageRepository } from './page.repository.interface';

export const PAGE_REPOSITORY = new InjectionToken<PageRepository>('STYLESHEET_REPOSITORY');
